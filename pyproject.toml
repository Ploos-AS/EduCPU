[project]
name = "educpu"
version = "0.5.0"
description = "Pedagogy-first virtual CPU and teaching toolchain"
requires-python = ">=3.11"

[project.scripts]
eduasm = "eduasm:main"
edudis = "edudis:main"
edusim = "edusim:main"
eduvis = "eduvis:main"
edulink = "edulink:main"
educ = "educ:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["reference", "tools", "emulator"]
